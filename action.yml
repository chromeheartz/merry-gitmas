name: "Merry Gitmas"
description: "Render a retro pixel-art christmas tree decorated from your GitHub contributions this year."
author: "chromeheartz"
branding:
  icon: "gift"
  color: "green"

inputs:
  github_token:
    description: "GitHub token used to read your contribution calendar. Pass secrets.GITHUB_TOKEN from your workflow."
    required: true
  username:
    description: "GitHub username to render. Defaults to the repository owner."
    required: false
  output:
    description: "Output SVG path, relative to the repo root."
    required: false
    default: "profile-tree.svg"
  theme:
    description: "Background theme: default (snowy night) or space (cosmos)."
    required: false
    default: "default"
  width:
    description: "Rendered image width in px (height auto from aspect ratio)."
    required: false
    default: "350"

runs:
  using: "node20"
  main: "dist/index.js"
